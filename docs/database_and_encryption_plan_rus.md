
## Stage 1 - Локальная база данных с шифрованием

---

### 1. Общая концепция Stage 1
**Цель:** Создать локальную базу данных (SQLite), которая **автоматически** шифрует конфиденциальные данные перед записью на диск и расшифровывает их при чтении.

**Главный принцип:** Код приложения (UI) работает с обычными объектами (Dart classes), а база данных хранит "черный ящик" (зашифрованные байты). Преобразование происходит прозрачно.

---

### 2. Ключевые компоненты

#### А. Модель данных (`TransactionData`)
Это чистый Dart-класс (используем `Freezed`), который содержит всё, что мы хотим скрыть:
*   Сумма (`amount`)
*   Описание (`description`)
*   Категория (`category`)
*   Валюта (`currency`)

**Важный нюанс:** В промпте сказано: *"Include ID and Date inside the JSON model to verify integrity"*.
Это значит, что внутри этого секретного JSON мы дублируем `id` и `date`.
*   **Зачем?** Это защита от подмены (Anti-Tampering). Если злоумышленник (или "баг" сервера) возьмет зашифрованный блоб от транзакции "Покупка кофе за $5" и подсунет его в строку с ID транзакции "Зарплата $5000", при расшифровке мы увидим:
    *   ID строки в БД: `uuid-1` (Зарплата)
    *   ID внутри JSON: `uuid-2` (Кофе)
    *   **РЕЗУЛЬТАТ:** Мы выбрасываем ошибку безопасности (`SecurityException`).

#### Б. Таблица базы данных (`Transactions`)
В базе данных Drift (SQLite) структура будет гибридной. Мы не шифруем всё подряд, иначе база станет бесполезной для сортировки и синхронизации.

| Колонка | Тип | Содержание | Зашифровано? | Зачем нужно? |
| :--- | :--- | :--- | :--- | :--- |
| `id` | UUID | Уникальный ID | **НЕТ** | Первичный ключ, нужен для связей. |
| `date` | Int | Unix Timestamp | **НЕТ** | Чтобы быстро сортировать список по дате (`ORDER BY date`) без расшифровки всех строк. |
| `last_updated_at` | Int | Timestamp | **НЕТ** | Для логики синхронизации (Last Write Wins). |
| `is_deleted` | Bool | Флаг удаления | **НЕТ** | "Мягкое удаление". Мы не стираем строку, а помечаем её удаленной для синхронизации. |
| `encrypted_blob` | Blob | Uint8List | **ДА** | Здесь лежит JSON `TransactionData`, зашифрованный алгоритмом AES-GCM (или ChaCha20). Для SQLite это просто набор случайных байт. |

#### В. Магия Drift: `TypeConverter`
Drift имеет механизм `TypeConverter`. Это "мост" между Dart и SQL.

1.  **При записи (INSERT/UPDATE):**
    *   Приложение дает объект `TransactionData`.
    *   Конвертер превращает его в JSON-строку.
    *   Конвертер берет **Ключ Шифрования** (пока что заглушку, позже — настоящий ключ).
    *   Шифрует JSON в байты (Ciphertext).
    *   Drift записывает эти байты в колонку `encrypted_blob`.

2.  **При чтении (SELECT):**
    *   Drift читает байты из `encrypted_blob`.
    *   Конвертер расшифровывает их ключом.
    *   Превращает JSON обратно в объект `TransactionData`.
    *   **Проверка целостности:** Сравнивает `id` из JSON с `id` из колонки.
    *   Приложение получает готовый объект.

**Почему это круто?** В коде репозитория вы просто пишете `database.select(transactions).get()`. Вы не думаете о шифровании каждый раз. Оно происходит само.

---

### 3. Ограничения этого подхода (Важно понимать)
Так как данные зашифрованы, SQL-движок (SQLite) их не "видит".

*   **Что НЕЛЬЗЯ делать:** `SELECT * FROM transactions WHERE amount > 1000`.
    *   SQLite не может сравнить зашифрованные байты с числом 1000.
*   **Как это решается:** Фильтрация происходит **в памяти** (In-Memory).
    1.  Загружаем транзакции (например, за текущий месяц, используя открытую колонку `date`).
    2.  Они автоматически расшифровываются.
    3.  В Dart-коде делаем `.where((t) => t.amount > 1000)`.
    *   *Для личных финансов (даже 50,000 транзакций) современные телефоны справляются с этим мгновенно.*

---

### 4. Desktop-специфика (Windows/Linux)
В задании упомянута библиотека `sqlite3_flutter_libs`.
*   На Android/iOS SQLite встроен в систему.
*   На Windows/Linux его часто нет "из коробки" или версия старая.
*   Эта библиотека кладет нужный файл `sqlite3.dll` (Windows) или `libsqlite3.so` (Linux) прямо в папку с вашей программой. Это гарантирует, что приложение не упадет с ошибкой "Dll missing" при запуске на компьютере.

---

### Итог Stage 1
В конце этого этапа у нас будет приложение, которое выглядит как обычный список дел/транзакций. Но если мы откроем файл базы данных (`.sqlite`) через "DB Browser for SQLite", мы увидим даты и ID, но вместо денег и описаний — **нечитаемую кашу из байтов**.



---------------------

## Stage 2 - Генерация и использование ключей шифрования

---

### Главная концепция: Иерархия Ключей

В обычных приложениях пароль пользователя отправляется на сервер для проверки. В нашем случае **пароль — это криптографический инструмент**, который никогда не должен покидать устройство в открытом виде (для целей шифрования).

Мы строим систему из двух уровней ключей:

1.  **Master Key (MK):**
    *   Это 32 байта абсолютно случайных данных (Random Bytes).
    *   Именно этим ключом шифруется вся локальная база данных (SQLite).
    *   **Важно:** Этот ключ никогда не меняется (пока мы не сделаем ротацию ключей в будущем), даже если пользователь сменит пароль.

2.  **Key Encryption Key (KEK) — Ключ для шифрования ключа:**
    *   Этот ключ *выводится* (derive) из пароля пользователя с помощью алгоритма **Argon2id**.
    *   Он нужен **только** для того, чтобы зашифровать Master Key перед отправкой его в облако.

---

### Детальный разбор задач (Tasks)

#### 1. CryptoService и Изоляты (Isolates)

**Задача:** Реализовать функцию `deriveKeyFromPassword(password, salt)`.

*   **Почему Argon2id?** Это алгоритм хеширования, устойчивый к перебору на видеокартах (ASIC/GPU resistance). Он требует много оперативной памяти и времени. Это защита от хакеров, которые украдут базу и попытаются подобрать пароли.
*   **Зачем `compute()` (Isolate)?** Хороший хеш Argon2id должен вычисляться около 0.5–1.0 секунды на современном телефоне. Если запустить это в главном потоке Flutter (Main Thread), интерфейс приложения "замерзнет" на секунду. Пользователь подумает, что приложение зависло. Поэтому мы выносим эту тяжелую математику в отдельный изолят.

#### 2. Auth Flow: Регистрация (Создание Хранилища)

Когда пользователь регистрируется, происходит следующее:

1.  **Генерация MK:** Приложение создает случайные 32 байта. Это наш "Святой Грааль".
2.  **Генерация KEK:** Пользователь вводит пароль. Приложение в изоляте превращает `Пароль + Случайная Соль` в `KEK`.
3.  **Создание `vault.json`:** Приложение берет MK и шифрует его с помощью KEK. Получается зашифрованный "blob".
4.  **Загрузка в Supabase:**
    *   Мы регистрируем пользователя в Supabase Auth (чтобы получить `user_id`).
    *   Мы загружаем этот зашифрованный blob (файл `vault.json`) в Supabase Storage.
    *   **Итог:** Сервер Supabase видит файл, но внутри него — мусор. Сервер не знает ни пароля, ни Master Key.

#### 3. Auth Flow: Логин (Новое устройство)

Пользователь купил новый телефон и хочет войти:

1.  **Supabase Auth:** Вводит email/пароль для входа в учетку Supabase. Это дает право *скачать* файл.
2.  **Скачивание:** Приложение скачивает `vault.json`.
3.  **Расшифровка:**
    *   Приложение просит ввести "Пароль шифрования" (обычно тот же, что и от аккаунта, но технически может быть другим).
    *   Снова запускается Argon2id -> получаем KEK.
    *   Пытаемся расшифровать `vault.json` с помощью KEK.
    *   **Успех:** Мы достали Master Key! Теперь можно расшифровать локальную базу данных.
    *   **Ошибка:** Если пароль неверный, расшифровка (AES-GCM) выдаст ошибку целостности (MAC check failed).

#### 4. Quick App Launch (Быстрый вход)

Вводить сложный пароль при каждом запуске приложения неудобно.

*   **Решение:** После успешного входа мы сохраняем **Master Key** в защищенное хранилище ОС.
    *   **iOS:** Keychain.
    *   **Android:** EncryptedSharedPreferences (Keystore).
    *   **Linux:** Libsecret (Gnome Keyring / KWallet). *Именно поэтому в требованиях указан `libsecret-1-dev`.*
    *   **Windows:** DPAPI (через библиотеку `flutter_secure_storage`).
*   **Логика:** При старте приложение проверяет SecureStorage. Если там есть MK — пускаем пользователя мгновенно (или просим биометрию). Если нет — просим пароль и идем за `vault.json`.

#### 5. "No Reset" Policy (Точка невозврата)

Это архитектурное следствие Zero-Knowledge.

*   **Проблема:** Если пользователь забыл пароль, он не может сгенерировать правильный KEK.
*   **Следствие:** Он не может расшифровать `vault.json`. Он не может достать Master Key. Данные в базе — это просто набор случайных байтов, которые невозможно восстановить.
*   **UI:** Мы обязаны показать страшное красное окно: *"Мы не храним ваш пароль. Если вы его забудете, ваши деньги и данные будут потеряны навсегда. Восстановить невозможно."*

---

### Почему это безопасно?

1.  **Сервер скомпрометирован:** Хакер скачивает `vault.json`. Без пароля пользователя (который не хранится на сервере) это бесполезно.
2.  **Перехват трафика:** Весь трафик HTTPS, но даже если SSL взломан, внутри лежат только зашифрованные блобы.
3.  **Подмена данных:** Мы используем аутентифицированное шифрование (AES-GCM). Если сервер попробует подменить байты в `vault.json`, при расшифровке на клиенте вылетит исключение, и приложение не примет поддельный ключ.

### Итог Stage 2

К концу этого этапа будет приложение, которое:
1.  Умеет "дробить" пароль тяжелой математикой, не тормозя UI.
2.  Хранит ключи так, что даже разработчик сервера не может прочитать данные.
3.  Работает удобно (биометрия/авто-вход) благодаря Secure Storage.
4.  Готово к синхронизации (так как ключи теперь есть и они одинаковы на всех устройствах одного юзера).

---------------------

## Stage 3 - Очередь синхронизации

---

### 🏛 Архитектура Stage 3: "Паттерн Outbox"

Представь, что твое приложение — это почтовое отделение.
В **Stage 1** мы построили склад (Локальную БД), где храним посылки.
В **Stage 3** мы строим **Журнал Исходящих (Outbox)**.

#### Проблема, которую мы решаем:
Если ты просто сохраняешь данные в таблицу `Transactions`, а интернета нет, как потом узнать, *что именно* нужно отправить на сервер, когда интернет появится?
1.  Можно сканировать всю базу и искать "новые" записи. (Долго, неэффективно).
2.  А если ты **удалил** запись? В локальной базе её больше нет. Как сервер узнает, что её надо удалить?

#### Решение: Таблица `SyncQueue`
Мы создаем отдельную таблицу, которая работает как **лог действий**. Каждое изменение данных (создание, редактирование, удаление) записывается в этот лог.

---

### 🛠 Детальный разбор задач Stage 3

#### 1. Таблица `SyncQueue` (Журнал)
Мы создаем таблицу в Drift (SQLite), которая хранит *намерения*.

*   **`id` (Auto-Inc):** Порядковый номер события. Гарантирует, что события уйдут на сервер в том же порядке, в котором произошли (сначала "Создал", потом "Отредактировал").
*   **`operation` (Enum):**
    *   `UPSERT` (Insert or Update): "Я сохранил данные".
    *   `DELETE`: "Я удалил данные".
*   **`row_id` (UUID):** ID конкретной транзакции, которую мы меняем.
*   **`table_name`:** Строка (например, "transactions"). Это нужно для масштабируемости. Если завтра ты добавишь таблицу "Счета" или "Категории", ты будешь использовать эту же очередь, просто меняя имя таблицы.
*   **`encrypted_payload` (Text/Blob):** Это **самое важное**.
    *   Мы шифруем данные (JSON с транзакцией) **сразу же в момент сохранения**.
    *   В очередь попадает уже зашифрованный "мусор".
    *   **Зачем?** Когда фоновый процесс (воркер) будет отправлять эти данные на сервер, ему **не нужно знать мастер-ключ**. Он просто берет зашифрованную строку и кидает её в облако. Это повышает безопасность.

#### 2. Transaction Manager (Диспетчер)
Приложение больше не должно писать в базу напрямую через `dao.insert()`. Мы создаем слой-посредник.

**Сценарий "Сохранить транзакцию" (Save):**
1.  Пользователь нажимает "Сохранить".
2.  Менеджер берет данные.
3.  **Действие 1 (Локальное):** Пишет в таблицу `Transactions` (чтобы на экране данные обновились мгновенно).
4.  **Действие 2 (Криптография):** Сериализует данные в JSON и шифрует их Мастер-ключом.
5.  **Действие 3 (Очередь):** Пишет новую строку в `SyncQueue`:
    *   Op: `UPSERT`
    *   Payload: `[Зашифрованный JSON]`

**Сценарий "Удалить транзакцию" (Delete):**
1.  Пользователь нажимает "Удалить".
2.  **Действие 1 (Локальное):** Ставит флаг `is_deleted = true` и **обновляет `last_updated_at`** (текущим временем).
3.  **Действие 2 (Криптография):** Сериализует **минимальный JSON** (содержащий `{id, last_updated_at, is_deleted: true}`) и шифрует его Мастер-ключом.
4.  **Действие 3 (Очередь):** Пишет строку в `SyncQueue`:
    *   Op: `DELETE`
    *   Payload: `[Зашифрованный JSON с меткой времени]`.
    *   *Важно:* Ранее была идея передавать `null`, но было принято решение передавать зашифрованную метку времени. Это критично, чтобы другое устройство не применило "старое удаление", если транзакция была отредактирована ("воскрешена") позже.


#### 3. Атомарность (Транзакции БД)
Это страховка от сбоев.
Представь:
1.  Мы записали транзакцию в `Transactions`.
2.  Батарейка села / приложение упало.
3.  В `SyncQueue` запись попасть не успела.

**Результат:** У пользователя на телефоне транзакция есть, но она **никогда** не уйдет на сервер, потому что в очереди её нет. Рассинхрон.

**Решение:** Мы оборачиваем запись в обе таблицы в одну SQL-транзакцию (`db.transaction(...)`).
*   Либо обе записи успешно сохраняются.
*   Либо (при ошибке) отменяются обе.
Это гарантирует целостность данных.



---------------------

## Stage 4 - Облачная синхронизация


---

### **1. Философия: "Append-Only Log" (Журнал событий)**

Обычно новички пытаются синхронизировать базы данных, просто загружая файл `db.sqlite` на сервер.
*   *Проблема:* Если у вас два телефона, и вы изменили данные на обоих, последний загруженный файл перезапишет изменения первого. Данные потеряются.

**Наше решение:** Мы используем подход **Event Sourcing** (Источник событий).
Мы не синхронизируем *состояние* (конечную таблицу), мы синхронизируем *действия* (события).
*   Пример: Вместо "На счету 100 рублей", мы отправляем "Пользователь добавил транзакцию на 50 рублей".

---

### **2. Таблица на сервере: `enc_event_log`**

В Supabase создается одна главная таблица, которая работает как "почтовый ящик" для всех устройств пользователя.

**Структура таблицы:**
1.  **`id` (BigInt, Auto-Increment):** Глобальный счетчик событий. Это критически важно. Благодаря этому устройство может сказать: *"Сервер, дай мне все, что случилось после события №1050"*.
2.  **`user_id` (UUID):** Чтобы разделять данные разных пользователей (через RLS).
3.  **`device_id` (Text):** Уникальный ID устройства, которое создало запись (например, "iPhone-Ivan"). Это нужно, чтобы не скачивать свои же изменения обратно (проблема "Эха").
4.  **`operation` (Text):** Тип действия (`UPSERT` или `DELETE`).
5.  **`row_id` (UUID):** ID конкретной транзакции, которую мы меняем.
6.  **`encrypted_body` (Text):** Самое главное. Это зашифрованный JSON (AES-GCM). Сервер видит, что "что-то изменилось", но не знает **что именно** (сумма, категория, описание — всё тут, внутри шифра).

**Безопасность (RLS Policies):**
*   **INSERT:** Разрешено только себе (`auth.uid() = user_id`).
*   **SELECT:** Разрешено только себе.
*   **UPDATE/DELETE:** **ЗАПРЕЩЕНО ВСЕМ.**
    *   *Почему?* Это журнал. Историю нельзя переписывать. Если вы хотите изменить транзакцию, вы не редактируете старую запись в журнале, а добавляете **новую** запись "Обновить транзакцию". Это гарантирует целостность.

---

### **3. Процесс PUSH (Отправка данных)**

Как данные попадают с телефона на сервер:

1.  **Чтение очереди:** Приложение смотрит в локальную таблицу `SyncQueue` (которую мы создали на Stage 3). Там лежат "намерения" (например, 5 новых транзакций).
2.  **Пакетирование (Batching):** Мы не шлем по одному запросу. Мы берем пачку (например, 50 штук) и формируем один запрос к Supabase. Это экономит заряд батареи и трафик.
3.  **Отправка (Insert):** Отправляем пачку в `enc_event_log`.
4.  **Стратегия Идемпотентности (Idempotency):**
    *   *Правило:* Мы удаляем записи из локальной `SyncQueue` **только** после того, как сервер ответил `HTTP 200 OK`.
    *   Если интернет пропал в момент отправки, записи остаются в очереди. При следующем запуске мы попробуем отправить их снова.

---

### **4. Процесс PULL (Получение данных)**

Как данные попадают с сервера на второй телефон:

1.  **Указатель (Cursor):** В `SharedPreferences` хранится число `last_synced_id` (например, 500). Это значит, что мы уже знаем обо всем, что случилось до 500-го события.
2.  **Запрос:** Мы говорим Supabase:
    > *"Дай мне записи из `enc_event_log`, где `id > 500` И `device_id != мой_текущий_device_id`".*
    *   *Фильтр `device_id`:* Это экономия. Если я сам отправил данные 5 минут назад, сервер не должен мне их возвращать. Я и так знаю, что я сделал.
3.  **Обработка (Loop):**
    *   Мы получаем список зашифрованных событий.
    *   **Decrypt:** Расшифровываем `encrypted_body` своим Мастер-Ключом.
    *   **Apply:**
        *   Если `UPSERT`: Вставляем или обновляем запись в локальной Drift-базе.
        *   Если `DELETE`: Помечаем запись как `is_deleted = true`.

---

### **5. Критически важная деталь: "Бесконечный цикл"**

Это самый тонкий момент архитектуры, на котором многие ломаются.

**Сценарий проблемы:**
1.  Сервер присылает новую транзакцию (PULL).
2.  Мы сохраняем её в локальную БД Drift.
3.  У нас есть "Перехватчик" (из Stage 3), который следит за БД: *"О, новая запись в БД! Надо добавить её в `SyncQueue`, чтобы отправить на сервер!"*
4.  **Результат:** Мы получили данные с сервера, записали их, тут же посчитали их "новыми", добавили в очередь и отправили обратно на сервер. Сервер вернул их снова. Бесконечный цикл.

**Решение (в задаче указано как "Disable Interceptor"):**
В коде репозитория (`TransactionManager`) должен быть специальный метод для синхронизации, например `saveFromSync()`.
*   Обычный `save()` -> пишет в БД -> пишет в `SyncQueue`.
*   `saveFromSync()` -> пишет в БД -> **ИГНОРИРУЕТ** `SyncQueue`.

---

### **Итог Stage 4**

После реализации этого этапа у вас будет:
1.  **Два симулятора (например, iOS и Android).**
2.  Вы создаете транзакцию на iOS. Она шифруется, падает в очередь.
3.  Нажимаете "Sync" (или он срабатывает сам). Данные улетают в Supabase в виде бинарной каши.
4.  На Android нажимаете "Sync". Он скачивает новые события (которых он еще не видел), расшифровывает их и показывает в списке.
5.  **Supabase** при этом выступает как "слепой курьер": он передает пакеты, но не знает, сколько денег внутри и на что они потрачены.

Это и есть настоящий **Zero-Knowledge Sync**.

---------------------

## Stage 5 - Разрешение конфликтов и полировка

---


### 1. Стратегия LWW (Last-Write-Wins / Последняя запись побеждает)

**Проблема:**
Представь ситуацию:
1.  У тебя есть транзакция "Покупка кофе" на **100 руб**.
2.  Ты **офлайн** на телефоне: меняешь сумму на **120 руб** (в 10:00).
3.  Ты **офлайн** на ноутбуке: меняешь сумму на **150 руб** (в 10:05).
4.  Оба устройства выходят в онлайн и отправляют данные на сервер.

Если просто применить оба изменения по очереди, результат будет зависеть от того, чей пакет пришел на сервер последним, или (что хуже) мы можем получить рассинхронизацию баз.

**Решение (LWW):**
Мы доверяем "Времени". Побеждает та версия данных, у которой метка времени (`last_updated_at`) новее.

**Как это реализовать технически:**
В **Stage 1** мы добавили поле `last_updated_at` в таблицу базы данных Drift.
В **Stage 5** мы модифицируем логику **Pull (Получения данных)**:

1.  Приложение скачивает событие (`UPSERT` или `DELETE`) с сервера.
2.  Оно **всегда расшифровывает** полезную нагрузку (payload). Даже если это удаление, нам нужно достать оттуда `incoming_timestamp`.
3.  Приложение делает запрос к локальной базе Drift: *"Какая метка времени сейчас у этой записи локально?"*
4.  **Сравнение (Resolution Rule):**
    *   Если `Incoming_Timestamp > Local_Timestamp`: **Применяем изменение**.
        *   Если это UPSERT — обновляем поля.
        *   Если это DELETE — ставим `is_deleted = true`.
    *   Если `Incoming_Timestamp < Local_Timestamp`: **Игнорируем событие**.
        *   Это значит, что у нас локально данные (или статус удаления) свежее, чем то, что пришло с сервера.
        *   *Пример защиты:* Устройство А удалило транзакцию в 10:00. Устройство Б отредактировало её в 10:05. При синхронизации Устройство Б увидит удаление от 10:00, поймет, что 10:00 < 10:05, и **проигнорирует удаление**. Транзакция останется "живой" и обновленной.

**Почему это важно для Zero-Knowledge:**
Сервер Supabase не знает, "какая" версия правильная, потому что данные зашифрованы. Логика разрешения конфликта должна происходить строго **на клиенте** после расшифровки.

---

### 2. Connectivity & Lifecycle (Связь и Жизненный цикл)

Приложение должно быть "живым". Пользователь не должен нажимать кнопку "Синхронизировать", чтобы увидеть актуальные данные.

**А. Connectivity Plus (Реакция на сеть):**
Мы используем пакет `connectivity_plus`. Он предоставляет поток (Stream), который сообщает: "Появился Wi-Fi" или "Появился мобильный интернет".
*   **Логика:** Как только статус сети меняется с `none` на `connected` -> мы автоматически запускаем процесс синхронизации (Sync Engine). Это гарантирует, что данные улетят в облако, как только пользователь выйдет из метро.

**Б. AppLifecycleListener (Фокус приложения):**
Пользователь свернул приложение (оно ушло в Background) и открыл его снова через час (Foreground). За этот час он мог внести изменения с другого устройства.
*   **Логика:** Используем `AppLifecycleListener` (встроен в Flutter). При событии `onResume` (выход из фона) -> принудительно запускаем **Pull** (скачивание изменений), чтобы пользователь сразу увидел актуальный баланс.

---

### 3. Sync Indicator (Визуальная индикация)

Пользователь должен доверять приложению. Финансовые данные — это чувствительная тема. Если я внес расход, я хочу знать, сохранен ли он.

**Задача:** Создать UI-компонент (обычно в AppBar или в углу экрана), который показывает три состояния:
1.  **Saved (Галочка / Зеленый):** Все локальные изменения успешно отправлены на сервер. Очередь `SyncQueue` пуста.
2.  **Syncing (Спиннер / Вращение):** В данный момент идет отправка или получение данных.
3.  **Offline / Pending (Облачко перечеркнуто или Желтый):** Есть изменения в `SyncQueue`, но нет интернета, чтобы их отправить.

Это чисто психологический, но критически важный аспект UX.

---

### 4. Критический момент: Блокировка "Эха" (Loop Prevention)

Это упоминалось в Stage 4, но в Stage 5 при внедрении автоматической синхронизации это становится местом, где чаще всего возникают баги.

**Сценарий бесконечного цикла:**
1.  Sync Engine скачивает обновление с сервера.
2.  Он пишет это обновление в базу Drift.
3.  Drift видит запись в базу и думает: *"О, данные изменились! Надо добавить это в очередь `SyncQueue` для отправки на сервер!"* (Срабатывает наш перехватчик из Stage 3).
4.  Sync Engine видит новую запись в очереди и отправляет её обратно на сервер.
5.  Сервер получает дубликат.

**Решение:**
В Stage 5 мы должны убедиться, что наш `LocalDatabaseRepository` или `TransactionManager` имеет флаг: `isSyncing`.
*   Когда мы пишем данные из UI -> `isSyncing = false` -> Добавляем в `SyncQueue`.
*   Когда мы пишем данные из Sync Engine (Pull) -> `isSyncing = true` -> **НЕ** добавляем в `SyncQueue`.

---

### Итог Stage 5

**Deliverable (Результат):**
У тебя на руках два симулятора (или телефон и симулятор).
1.  Ты выключаешь интернет на обоих.
2.  На Устройстве А меняешь транзакцию: "Кофе" -> 150р.
3.  На Устройстве Б меняешь ту же транзакцию: "Кофе" -> 200р (делаешь это на минуту позже).
4.  Включаешь интернет на обоих.
5.  Оба устройства синхронизируются.
6.  **Магия:** На обоих устройствах сумма становится 200р (потому что это изменение было последним по времени). Никаких дубликатов, никаких ошибок расшифровки.

Это завершающий штрих, который делает из "базы данных с шифрованием" полноценный "облачный финансовый менеджер".


---------------------

## Stage 6

---


### 1. Snapshotting (Снэпшоты / Сжатие логов)

В нашей архитектуре мы используем подход **Event Sourcing (или Append-Only Log)**. Это значит, что мы синхронизируем не *состояние* базы (файл `db.sqlite`), а *историю изменений* (события: "добавил запись", "удалил запись", "изменил сумму").

#### Проблема: "Раздувание" истории
Представь, что пользователь пользуется приложением 2 года.
*   У него 5,000 транзакций.
*   Он часто редактирует записи, удаляет и создает новые.
*   В таблице синхронизации `enc_event_log` может скопиться **50,000 событий**.

Когда пользователь покупает новый телефон и логинится:
1.  Без сэмпшотов приложение должно скачать все 50,000 событий по очереди.
2.  Оно должно "проиграть" их (replay) локально: вставить, потом обновить, потом удалить...
3.  Это **долго** (пользователь ждет загрузки 5-10 минут) и **дорого** (много чтений из базы Supabase и трафика).

#### Решение: Снэпшоты (Checkpoints)
Мы внедряем механизм "сохранения состояния", как в видеоиграх. Вместо того чтобы проходить игру с 1-го уровня каждый раз, мы загружаем сохранение на 50-м уровне.

**Как это работает технически:**
1.  **Генерация:** Раз в неделю (или при накоплении 1000 событий) устройство берет текущую локальную базу данных Drift/SQLite.
2.  **Дамп:** Сериализует все актуальные данные (без удаленных записей) в один большой JSON-файл или бинарный blob.
3.  **Шифрование:** Шифрует этот файл целиком Мастер-ключом (MK).
4.  **Загрузка:** Загружает этот файл в **Supabase Storage** (файловое хранилище дешевле базы данных).
5.  **Метка:** В таблицу базы данных пишет запись типа `SnapshotPointer`:
    *   `file_path`: "user_id/backup_2026_05_20.bin"
    *   `last_event_id`: 50,000 (ID последнего обработанного события на момент создания дампа).

**Сценарий нового устройства:**
1.  Приложение проверяет, есть ли `SnapshotPointer`.
2.  Если есть — скачивает **один** файл бэкапа.
3.  Расшифровывает и сразу заполняет базу данных Drift (это секунды).
4.  Запрашивает у Supabase события **только с ID > 50,000**.
5.  Итог: Скачали 1 файл + 50 новых событий вместо 50,000 старых событий.

---

### 2. Key Rotation (Ротация ключей)

В системах с шифрованием (E2EE) есть правило хорошего тона: **криптографические ключи нужно периодически менять**.
*   Вдруг пользователь подозревает, что его пароль подсмотрели?
*   Вдруг алгоритм шифрования устарел?

#### Проблема: Сервер слеп
В обычных приложениях (банковских) вы меняете пароль, и сервер перешифровывает ваши данные своим ключом. Но у нас **Zero-Knowledge**: сервер видит только "мусор" (шифротекст) и не имеет ключа. Сервер **не может** перешифровать данные за вас.

#### Решение: Клиентская перешифровка (Re-encryption)
Это сложный процесс, который требует полного перезаписывания данных.

**Алгоритм смены ключа:**
1.  **Генерация:** Приложение создает **Новый Мастер-Ключ (MK_New)**.
2.  **Чтение:** Приложение расшифровывает все локальные данные, используя **Старый Ключ (MK_Old)**. Данные теперь в оперативной памяти в открытом виде.
3.  **Запись:** Приложение шифрует эти данные с помощью **MK_New** и сохраняет обратно в локальную базу.
4.  **Инвалидация лога:**
    *   Все старые события в `enc_event_log` на сервере зашифрованы старым ключом (`MK_Old`). Они теперь бесполезны, так как мы "выкидываем" старый ключ.
    *   Мы не можем просто отправить новые события поверх старых — возникнет конфликт версий.
5.  **Создание Снэпшота (связь с пунктом 1):**
    *   Единственный способ синхронизировать это изменение с другими устройствами — создать **новый Снэпшот**, зашифрованный уже `MK_New`.
    *   Приложение загружает этот "свежий старт" на сервер.
    *   Старый лог событий удаляется или помечается как устаревший (архивируется).
6.  **Другие устройства:**
    *   Устройство B (например, планшет) при попытке синхронизации обнаружит, что его ключ `MK_Old` больше не подходит к новым данным.
    *   Оно скачает новый зашифрованный паролем пользователя "конверт" с ключом `MK_New`.
    *   Оно увидит "Hard Reset" (новый снэпшот), удалит свою локальную базу и скачает актуальную версию, зашифрованную новым ключом.


### 3. **Настоящая совместная работа (CRDT):**
*   *Проблема:* Текущая стратегия LWW (Последняя запись побеждает) достаточна для одного пользователя, но несет риск потери данных в многопользовательских сценариях (например, если Пользователь А меняет "Сумму", а Пользователь Б одновременно меняет "Категорию", одно из изменений будет перезаписано).
*   *Решение:* Мигрировать внутреннюю структуру данных с простого JSON на **JSON CRDT** (Бесконфликтные реплицируемые типы данных).
    *   **Слияние на стороне клиента:** Так как сервер не видит данных, логика слияния переносится на Клиент. Когда приходит событие `UPSERT`, клиент расшифровывает payload и использует CRDT-библиотеку для математического слияния изменений на уровне полей (например, сохраняя *и* новую Сумму, *и* новую Категорию).
    *   **Векторные часы:** Заменить простую метку времени `last_updated_at` на "Векторные часы" (Vector Clock, например `{"DeviceA": 10, "DeviceB": 5}`), чтобы точно отслеживать причинно-следственную историю изменений внутри зашифрованного блоба.

*   **LWW** — это стратегия "Победитель получает всё" (на уровне строки).
*   **CRDT** — это стратегия "Ничего не теряем, всё объединяем" (математическое слияние).

#### 1. Сценарий конфликта (LWW vs CRDT)

Представьте ситуацию:
*   **Устройство А (Offline):** Пользователь меняет **Сумму** транзакции с 500 на 600.
*   **Устройство Б (Offline):** Пользователь меняет **Категорию** той же транзакции с "Еда" на "Ресторан".
*   Оба выходят в онлайн.

##### Как это работает сейчас (LWW):
Система смотрит на `last_updated_at`.
*   Если Устройство А сделало изменение в 10:00, а Устройство Б в 10:05 — **побеждает Б**.
*   **Результат:** Категория станет "Ресторан", но сумма **вернется к 500**. Изменение суммы от Устройства А будет полностью перезаписано и **потеряно**.
*   *Почему это приемлемо для нас?* В личных финансах редко два человека правят одну и ту же запись одновременно.

##### Как бы это работало с CRDT:
CRDT (в частности, Map-CRDT) умеет сливать изменения *по-поле-но*.
*   Оно видит: "Ага, устройство А меняло поле `amount`, а устройство Б меняло поле `category`. Эти изменения не противоречат друг другу".
*   **Результат:** Сумма станет 600, а Категория станет "Ресторан". Оба изменения сохранены.

#### 2. Как изменилась бы База Данных (SQLite)?

С CRDT мы не можем просто хранить текущее значение. Нам нужно хранить метаданные для **каждого поля**.

**Текущая таблица (LWW):**
```sql
CREATE TABLE transactions (
    id TEXT PRIMARY KEY,
    amount INT,
    category TEXT,
    last_updated_at INT -- Одно время на всю строку
);
```

**Таблица для CRDT (вариант реализации):**
Нам пришлось бы использовать JSON или отдельные колонки для каждого поля, чтобы хранить "Векторные часы" (Vector Clocks) или временные метки *для каждого поля*.

```sql
CREATE TABLE transactions_crdt (
    id TEXT PRIMARY KEY,
    -- Каждое поле — это сложный объект
    amount_value INT,
    amount_updated_at INT, 
    amount_device_id TEXT, -- Кто последний менял именно amount

    category_value TEXT,
    category_updated_at INT,
    category_device_id TEXT, -- Кто последний менял именно category

    is_deleted BOOL
);
```
*В реальности CRDT часто хранят вообще всю историю изменений (дельта-лог), что раздувает базу данных.*

#### 3. Как изменилось бы Шифрование (Zero-Knowledge)?

Это самая сложная часть.

**Сейчас (LWW):**
Мы шифруем весь JSON `TransactionData` целиком. При синхронизации мы просто заменяем старый зашифрованный блоб новым.

**С CRDT:**
Чтобы CRDT сработал, нам нужно на клиенте расшифровать *оба* конфликтующих состояния и слить их.

1.  **На сервере:** Сервер всё так же слеп. Он не может слить `Blob A` и `Blob B`, потому что не видит полей.
2.  **На клиенте:**
    *   Прилетает изменение с сервера.
    *   Мы **не можем** просто перезаписать локальную строку.
    *   Мы должны:
        1.  Расшифровать локальную запись.
        2.  Расшифровать пришедшую с сервера запись.
        3.  Сравнить timestamp каждого поля (`amount`, `category`, `description`).
        4.  Собрать новый объект-гибрид.
        5.  Зашифровать его заново.
        6.  Сохранить.



---------------------



## Architecture Notes & Rationale - Архитектурные заметки и обоснование

---

### 1. Почему Drift (SQLite) + Свой Sync, а не стандартный Supabase Offline?

*   **Проблема:** Стандартные SDK (Firebase, Supabase) работают просто: "синхронизируй эту строку JSON". Но чтобы синхронизировать, они должны *читать* данные (сравнивать поля).
*   **Конфликт:** У нас данные зашифрованы (AES-GCM). Сервер видит только "кашу" из байтов. Он не может понять, какая версия новее или как объединить данные.
*   **Решение:**
    1.  **Drift (SQLite):** Дает нам **ACID** транзакции локально. В финансах нельзя допустить, чтобы деньги списались со счета А, но не зачислились на счет Б из-за сбоя записи. SQLite гарантирует атомарность.
    2.  **Свой Sync:** Мы сами пишем логику транспорта. Мы отправляем на сервер зашифрованный "контейнер". Серверу не нужно знать содержимое, чтобы сохранить его.

### 2. Почему "Append-Only Log" (Журнал событий), а не перезапись файлов?

*   **Проблема (Race Condition):** Представь, что ты и твоя жена одновременно редактируете бюджет с двух телефонов без интернета. Если бы мы просто загружали файл `database.sqlite` на сервер, то победил бы тот, кто загрузил последним. Данные второго человека полностью стерлись бы.
*   **Решение:** Мы используем подход **Event Sourcing** (почти).
    *   Ты не перезаписываешь базу. Ты добавляешь запись в журнал: *"Событие #101: Я купил кофе"* (зашифровано).
    *   Жена добавляет: *"Событие #102: Я оплатила интернет"* (зашифровано).
    *   Сервер просто складывает эти события в список. Никто никого не перезаписывает.
    *   Устройства скачивают новые события и "проигрывают" их у себя, обновляя локальный SQLite.
*   **Бонус:** Это экономит трафик. Загрузить 500 байт JSON дешевле, чем 10 МБ базы данных.

### 3. Почему UUID вместо Integer (1, 2, 3...)?

*   **Проблема:** В оффлайне два устройства создают новую транзакцию. Если использовать авто-инкремент (1, 2, 3), оба устройства создадут транзакцию с `ID = 5`. При синхронизации произойдет конфликт ID.
*   **Решение:** **UUID v4** (Глобально уникальные идентификаторы). Вероятность совпадения астрономически мала. Устройства могут создавать ID независимо друг от друга, не спрашивая сервер.

### 4. Зачем нужна отдельная таблица `SyncQueue`?

*   **Проблема (Потерянные удаления):** Ты удалил транзакцию в UI. В локальной таблице `Transactions` её больше нет. Как при появлении интернета приложение вспомнит, что нужно сказать серверу "Удали ID 123"?
*   **Решение:** Мы разделяем **Состояние UI** и **Состояние Сети**.
    1.  Ты удаляешь транзакцию -> она исчезает из таблицы `Transactions` (или помечается `is_deleted`).
    2.  ОДНОВРЕМЕННО в таблицу `SyncQueue` кладется "письмо" для сервера: *"Операция: DELETE, ID: 123"*.
    3.  `SyncQueue` — это исходящая почта. Пока сервер не ответит "ОК, принял", письмо лежит в очереди. Это гарантирует доставку изменений.

### 5. Гибридное шифрование: Почему даты и время — открытый текст?

*   **Компромисс:** Безопасность vs Удобство.
*   **Ситуация:** Нам нужно сказать серверу: *"Дай мне все изменения, произошедшие после Вчера 15:00"*.
*   **Решение:**
    *   Поля `amount`, `category`, `description` — **зашифрованы** в blob. Никто не знает, сколько денег и на что потрачено.
    *   Поля `date`, `last_updated_at` — **открыты**.
    *   **Почему это допустимо:** Злоумышленник (или Supabase) увидит, что *какая-то* транзакция была обновлена *вчера в 15:00*. Но он не узнает суть транзакции. Это называется "утечка метаданных", и в данном случае это приемлемая плата за возможность быстрой синхронизации.
    *   **Поиск:** Поиск по тексту ("кофе") работает только локально. Мы расшифровываем данные в памяти телефона и фильтруем там. SQL не может искать внутри шифротекста.

### 6. Зачем Argon2id + `vault.json`? (Схема с Мастер-ключом)

*   **Проблема:** Если мы зашифруем базу паролем пользователя ("12345"), а он потом захочет сменить пароль на "qwerty", нам придется **перешифровать гигабайты данных** (весь SQLite).
*   **Решение (Key Wrapping):**
    1.  Генерируем случайный 32-байтный **Master Key (MK)**. Им шифруем базу. Он никогда не меняется.
    2.  Шифруем сам **MK** паролем пользователя с помощью алгоритма **Argon2id** (он очень медленный и тяжелый, чтобы хакеры не могли подобрать пароль перебором).
    3.  Кладем зашифрованный MK в файл `vault.json` в облако.
    4.  Если юзер меняет пароль -> мы просто перешифровываем маленький `vault.json`. База данных остается нетронутой.
*   **Isolates:** Argon2id специально сделан так, чтобы грузить процессор на 100-500мс. Если запустить это в главном потоке Flutter, интерфейс зависнет. Поэтому мы строго используем `compute()` (Изоляты).

### 7. Зачем `FlutterSecureStorage` + `sqlite3_flutter_libs`?

*   **Проблема (Desktop):**
    *   Flutter на Windows/Linux часто падает, потому что в системе нет библиотеки `sqlite3.dll` или `libsecret`.
    *   Вводить пароль каждый раз при входе — неудобно.
*   **Решение:**
    *   `sqlite3_flutter_libs`: Мы "носим свою библиотеку с собой". Приложение не зависит от того, что установлено у пользователя в Windows.
    *   `SecureStorage`: Мы храним расшифрованный Мастер-ключ (MK) в защищенном хранилище ОС (Keychain на Mac, libsecret на Linux). Приложение запускается, достает ключ из "сейфа" ОС и сразу готово к работе. Без пароля (до смены сессии).

### 8. Проверка целостности (Client-Side Validation)

*   **Атака "Подмена" (Swap Attack):** Допустим, на сервере злой хакер. У вас есть две транзакции:
    1.  ID: A, Сумма: 10 руб. (Зашифрованный Blob X)
    2.  ID: B, Сумма: 1.000.000 руб. (Зашифрованный Blob Y)
    Хакер меняет их местами. Теперь в строке ID: A лежит Blob Y. Вы открываете "Кофе за 10 руб", а там миллион.
*   **Защита:** Мы кладем `ID` внутрь JSON **перед** шифрованием.
    *   При расшифровке мы смотрим: `Row ID` (из базы) == `JSON ID` (из расшифрованного блоба)?
    *   Если они не совпадают — бросаем `SecurityException`. Это гарантия, что никто не подменил зашифрованные данные.

Вот подробное, архитектурно обоснованное описание 9-го пункта на русском языке. Оно раскрывает суть конфликта между "Нулевым разглашением" (Zero-Knowledge) и сложными алгоритмами слияния данных.

Вставьте этот текст в раздел **Architecture Notes & Rationale**:

---

### 9. Почему выбрана стратегия LWW (Last-Write-Wins), а не CRDT?

Мы отказались от CRDT (Conflict-free Replicated Data Types) в пользу LWW не из-за лени, а из-за фундаментального конфликта между **шифрованием** и **структурой данных**.

1.  **Проблема "Атомарного Блоба" (The Encryption Friction):**
    *   *Суть:* В нашей архитектуре Zero-Knowledge мы шифруем весь объект `TransactionData` (сумму, категорию, описание) в один неделимый бинарный массив (Blob), используя AES-GCM. Для базы данных и сервера этот блоб — "черный ящик".
    *   *Конфликт:* CRDT работают на уровне *полей*. Чтобы автоматически слить изменения (например, Алиса изменила `сумму`, а Боб изменил `категорию`), алгоритм должен видеть оба поля. Но они зашифрованы внутри разных блобов.
    *   *Сложность решения:* Чтобы реализовать CRDT здесь, клиенту пришлось бы скачивать *все конфликтовавшие версии* блобов, расшифровывать их в памяти, запускать сложную логику слияния JSON-полей и заново шифровать результат. LWW позволяет нам трактовать зашифрованный блоб как атомарный файл: "Более новый файл целиком заменяет старый". Это радикально упрощает клиентскую логику.

2.  **Избыточность схемы БД (Schema Overhead):**
    *   Обычная SQL-таблица хранит значение: `amount = 500`.
    *   Таблица для CRDT обязана хранить метаданные для *каждого* поля: `amount_value`, `amount_timestamp`, `amount_author_id`. Это необходимо для разрешения конфликтов на уровне полей.
    *   Внедрение такой структуры в SQLite (Drift) превратило бы простую таблицу транзакций в сложного монстра или заставило бы хранить гигантские JSON-структуры с метаданными, что убивает производительность на мобильных устройствах.

3.  **Прагматизм бизнес-логики (Business Context):**
    *   Приложения вроде Google Docs или Figma *обязаны* использовать CRDT, так как их суть — одновременное редактирование.
    *   Для финансового менеджера ситуация, когда муж и жена редактируют *одну и ту же транзакцию* ровно в одну и ту же секунду (один меняет сумму, другой — категорию), является статистически ничтожной.
    *   Цена реализации CRDT (увеличение сложности кода в 3-4 раза) несоразмерна пользе (спасение 0.01% правок). Стратегия "Кто последний сохранил, тот и прав" интуитивно понятна пользователю в данном контексте.

### 10. RLS (Row Level Security) при шифровании

*   **Вопрос:** Зачем защищать строки на сервере, если они и так зашифрованы?
*   **Ответ (Defense in Depth):** Шифрование защищает от *чтения*. RLS защищает от *удаления/порчи*.
    *   Без RLS любой авторизованный пользователь мог бы отправить запрос `DELETE FROM enc_event_log` и стереть чужие данные (даже не зная, что там).
    *   RLS гарантирует: удалять и писать в свою очередь может только владелец `user_id`.

### 11. JSON внутри шифрованного Blob

*   **Гибкость:** Сегодня у нас поля `amount, desc`. Завтра мы захотим добавить `tags`.
*   Если бы это были колонки SQL, нам пришлось бы писать сложные миграции (`ALTER TABLE...`), шифровать старые данные по-новому и т.д.
*   Храня JSON внутри шифра, мы просто добавляем поле в Dart-модель. Если в старых записях его нет — будет `null`. Это упрощает обновление приложения.

### 12. Фильтр по `device_id` (Проблема Эха)

*   **Ситуация:**
    1.  Телефон А отправляет изменение на сервер.
    2.  Телефон А тут же спрашивает сервер: "Что нового?".
    3.  Сервер отдает Телефону А *его же изменение*.
*   **Решение:** При запросе обновлений ("Pull") мы говорим: *"Дай мне все события, ГДЕ `device_id` != `мой_device_id`"*.
*   Так мы не тратим трафик и ресурсы на обработку собственных изменений, которые и так уже есть в локальной базе.

---
